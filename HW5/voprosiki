1) Для работы с итерируемыми объектами и итераторами нужно использовать методы __iter__(),__next__().
2) Итерируемый объект должен быть счётным (~ множеству натуральных чисел, то, что можно посчитать, символы, слова, числа,..), конечным (но не всегда, как оказалось на семинаре),
упорядоченным (элементам объекта можно присвоить переменную итерации)
3) Итератор знает, какой элемент следующий, а генератор нет -- он его сам считает по заданному алгоритму, "генерирует".
4) чтобы вернуть значение генератора, пишем yield
